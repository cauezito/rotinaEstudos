{{#each errors}}
    <div class="alert alert-danger">{{text}}</div>
{{/each}}
<div class="card">
  <div class="card-body">
  <form class="mt-3" method="post" action="/study/save">
  <h3>Para criar um novo tópico de estudo, é necessário fornecer algumas informações...</h3>
      <label for="title" class="col-form-label">Título:</label>
        <input type="text" class="form-control"
         placeholder="Insira um título que descreva o tema do seu tópico de estudo." 
        name="title">
      <label for="description" class="col-sm-2 col-form-label">Descrição:</label>
        <input type="text" class="form-control" 
        placeholder="Uma breve descrição é sempre útil!"
        name="description">
      <label for="category">Categoria:</label>
        <select name="category" class="custom-select">
          {{#each categories}}
            <option value="{{_id}}">{{name}}</option>
          {{else}}
            <option value="0">Nenhuma categoria registrada</option>
          {{/each}}
        </select>
      <label for="content" class="col-sm-2 col-form-label">Conteúdo:</label>
      <textarea class="form-control" name="content" rows="5"></textarea>
      <div class="form-group row">
          <button class="btn btn-success btn-details col-2" type="submit">Criar</button>
          <button class="btn btn-warning btn-details col-2" type="reset">Limpar</button>
      </div>
  </form>
  </div>
</div>
